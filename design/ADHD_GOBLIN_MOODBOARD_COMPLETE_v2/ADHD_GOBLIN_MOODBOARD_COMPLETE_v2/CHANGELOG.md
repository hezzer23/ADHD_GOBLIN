# Changelog

## v2

Added three high-priority references to the source atlas:

- **Motes — Pulse** — live ASCII organism / reactive nervous tissue.
- **Motes** — implementation-oriented procedural ASCII background system.
- **Edoardo Lunardi** — workstation shell, pane structure, inspector, infinite canvas and interaction notes.

The package is now portable: local assets are included, and `build_moodboard.py` reads the updated `sources.json` before generating the HTML.
